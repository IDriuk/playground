Find ip to connect from wifi

ip a | grep inet

ifconfig | grep inet
Look for an IP similar to 192.168.x.x.

Windows (CMD or PowerShell):
ipconfig



// Create ssh for github, publish to gh-pages, add storybook to vite

ssh-keygen -t ed25519 -C "your_email@example.com"
cp -r ./ /app
root/.ssh
~/.ssh
yarn add gh-pages --dev

git config --global user.email "veytarn@gmail.com"
git config --global user.name "Ivan Driuk"

update home in package.json

yarn deploy

Repo settings / choose gh-pages

npx storybook@latest init --builder vite